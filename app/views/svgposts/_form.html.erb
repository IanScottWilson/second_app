<%= form_for(@svgpost) do |f| %>
  <% if @svgpost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@svgpost.errors.count, "error") %> prohibited this svgpost from being saved:</h2>

      <ul>
      <% @svgpost.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
